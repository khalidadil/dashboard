export default React.createClass({
  displayName: 'Skeleton',

  // propTypes: {
    // page: React.PropTypes.string,
    // updateTitle: React.PropTypes.func.isRequired,
  // },

  // componentDidMount() {
  //   this.props.updateTitle(this.props.route.path);
  // },

  // componentDidUpdate() {
  //   this.props.updateTitle(this.props.route.path);
  // },

  render() {
    return (
      <h1>This is a placeholder page.</h1>
    );
  },
});
